# Required outside local fixture mode. Must be an absolute HTTPS origin.
STAFF_API_ORIGIN=https://api.example.com

# Optional local-only deterministic authentication adapter.
# This is ignored when NODE_ENV=production.
STAFF_AUTH_MODE=fixture
